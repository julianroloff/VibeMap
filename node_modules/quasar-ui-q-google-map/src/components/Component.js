import { h } from 'vue'
import { QBadge } from 'quasar'

export default {
  name: 'QGoogleMap',

  setup () {
    return () => h(QBadge, {
      class: 'QGoogleMap',
      label: 'QGoogleMap'
    })
  }
}
